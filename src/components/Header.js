import { Link } from 'react-router-dom';

const Header = props => {
    // console.log(props, 'are header props')
    return (
        <header>
            <h1>Header Page</h1>
        </header>
    )
}

export default Header;